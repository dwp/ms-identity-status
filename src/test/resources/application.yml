spring:
  data:
    mongodb:
      database: identity
  main:
    banner-mode: off

schedule:
  reset:
    registrations:
      count: 0-59/5 * * * * *

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5
