logging:
  level:
    root: INFO

spring:
  main:
    banner-mode: 'off'

server:
  port: 8080

feature:
  mongo:
    versioned:
      api:
        enabled: true
  encryption:
    messaging:
      enabled: false

application:
  manager:
    base:
      url: /
max:
  timeout:
    millis: 29000
